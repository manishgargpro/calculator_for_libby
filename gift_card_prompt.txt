add a gift card function in the gift card menu space that has all of the following functionality: [entries are kept in a room database that include: [customer name; customer id (assigned sequentially by app); customer phone; customer email; transaction history that includes dates and charge totals; starting dollar amount from very beginning; remaining dollar amount after all transactions]; entries are searchable and viewable by customer name and/or email/phone; top-level gift card page shows scrollable list of entries with the following features: [ rows look like this: [ "<customer_id>: <customer_name>, <customer_phone/email>]; list is sorted by default by customer name alphabetically; if customer entry includes both phone and email, default to showing phone only]; top-level gift card page header has the following features: [existing "<Back" button; search function; "Add New" button which makes a new "New Customer" modal appear]; selecting an existing customer entry navigates to a page that displays the customer's entire transaction history which has the following features: [transaction history entry rows look like this: ["<date_and_timestamp>: <transaction_dollar_amount>"]; header of transaction history page looks like this: ["<customer_id>: <customer_name>, Started With <starting_dollar_amount>, Now Has <remaining_dollar_amount>]]; functions of "New Customer" modal are as follows: [text box entries for: [customer name (required); phone number (optional only if email exists); email (optional only if phone number exists)]; "Add" and "Cancel" buttons at the bottom with the following functions: ["Cancel" button clears all text boxes and exits the modal; "Add" button creates a new customer entry with included information and empty transaction history then exits the modal then navigates to the new customer's empty transaction history]]; customer entries are able to be added and/or edited and/or deleted with the following considerations: [deleting a customer entry deletes everything about that customer, including their id, name, phone, email, and entire transaction history; deleting a customer entry requires second confirmation of an "Are you sure?" pop-up with "Yes" or "No" options]; entries in transaction history are able to be edited and/or deleted with the following considerations: [deleting a transaction entry edits the remaining dollar amount for the customer attached to that transaction history; deleting a transaction entry requires second confirmation of an "Are you sure?" pop-up with "Yes" or "No" options]; transaction entries can be added by following these steps: [1 - choose items before navigating to the gift card page so that a total exists before choosing a customer entry; 2 - navigate to the gift page space afterwards; 3 - select a customer (should navigate to transaction history); 4 - press the "charge" button (can verify that total appends to transaction history with new date and timestamp)]; change "gift card" button to be disabled when total is zero or below; changes to "charge" button as follows: [only enabled in the following contexts: [outside of the gift card page; inside a transaction history if total is less than remaining amount]; inside a transaction history it also functions as a transaction entry append button that uses the current date and timestamp as well as current total; base functionality remains the same (still clears total but now if in gift card context only after appending a new transaction entry)]; the entire gift card function is completely offline and requires no internet connection whatsoever]