mutli-category tier system
  category of item (coffees, pastries, etc.)
  name of item (latte, cappuccino, etc.)
  modifier (sm/md/lg, milk type, syrup, etc.)
    these should be toggles
    subtotal list should include these modifiers (that you can also add/remove)
    modifier values are additive only, item default is known to POS users
  categories should lead to different pages of items
  item buttons should open to modifiers
quantity modifier
change checkbuttons to just buttons
all clicked items should be in a list that you can remove as necessary from
  clear button can just clear from the list
discount buttons (bring-your-own-mug  50 cent discount, etc.)
25 cent button and 1 dollar button
checkout button (may not be necessary right away)
csv support
  need a way to add new items and prices from the app itself
  where will the csv file be kept?
  look into backend solutions for mobile apps

for now, just port what exists to be able to be on an android tablet
  force app to be landscape orientation

[Dec 19 2025]
categorization is 3 tiers in: main category >> item >> modifiers
  keep an item total somewhere (visible would be nice) that you can zero as necessary when added to the total
"added items" area should be narrower and more to the right
modifiers to added items should also populate underneath the original item
buttons should be in grid, list will be left to right then down
  buttons should also be rectangular for easier fitting into UI
  main category buttons should be highlighted or not depending on which menu is open
  (see video for full UI workflow, including modifiers)
  also, see email for color palatte
switch "reset" button and "total" to be on opposite sides of where they are
add subtle lines between UI elements for cleaner look

[Dec 20 2025]
still to do:
  "EXTRA" menu needs proper item instantiation
  all items need proper prices
  currently only one type of modifier window is available, custom modifier windows still to come

[Dec 21 2025]
custom modifers (all should be achievable through boolean)
  base coffee needs some extra modifiers
grab and go (G&G) modifiers are their own items
in document, double starred (**) items have different size modifiers
SAMMI (sandwich), BB (bagel) and FCRO (croissant) have their own bespoke modifier lists, see document
MUFF (muffin) all same price but need modifiers anyway (don't need this right away)

[Dec 31 2025]
ask libby about prices for:
WELL
COLD (not CB)
Bagel ([Jan 1 2026]: this is BB)
BALLZ
BB ([Jan 1 2026]: added prices)
EMPAN
T SHI
MUG
BULK
HONEY

[Jun 08 2026]
Time to add a gift card function in the gift card menu space that has all of the following functionality:
  entries are kept in a room database that include:
    customer name
    customer id (assigned sequentially by app)
    customer phone
    customer email
    transaction history that includes dates and charge totals
    starting dollar amount from very beginning
    remaining dollar amount after all transactions
  entries are searchable and viewable by customer name and/or email/phone
  top-level gift card page shows scrollable list of entries with the following features:
    rows look like this: "<customer_id>: <customer_name>, <customer_phone/email>
    list is sorted by default by customer name alphabetically
    if customer entry includes both phone and email, default to showing phone only
  top-level gift card page header has the following features:
    existing "<Back" button
    search function
    "Add New" button which makes a new "New Customer" modal appear
  selecting an existing customer entry navigates to a page that displays the customer's entire transaction history which has the following features:
    transaction history entry rows look like this: "<date_and_timestamp>: <transaction_dollar_amount>"
    header of transaction history page looks like this: "<customer_id>: <customer_name>, Started With <starting_dollar_amount>, Now Has <remaining_dollar_amount>
  functions of "New Customer" modal are as follows:
    text box entries for:
      customer name (required)
      phone number (optional only if email exists)
      email (optional only if phone number exists)
    "Add" and "Cancel" buttons at the bottom with the following functions:
      "Cancel" button clears all text boxes and exits the modal
      "Add" button creates a new customer entry with included information and empty transaction history, then exits the modal, then navigates to the new customer's empty transaction history
  customer entries are able to be added and/or edited and/or deleted with the following considerations:
    deleting a customer entry deletes everything about that customer, including their id, name, phone, email, and entire transaction history
    deleting a customer entry requires second confirmation of an "Are you sure?" pop-up with "Yes" or "No" options
  entries in transaction history are able to be edited and/or deleted with the following considerations:
    deleting a transaction entry edits the remaining dollar amount for the customer attached to that transaction history
    deleting a transaction entry requires second confirmation of an "Are you sure?" pop-up with "Yes" or "No" options
  transaction entries can be added by following these steps:
    1) choose items before navigating to the gift card page so that a total exists before choosing a customer entry
      "gift card" button should be disabled when total is zero or below
    2) navigate to the gift page space afterwards
    3) select a customer (should navigate to transaction history)
    4) press the "charge" button (can verify that total appends to transaction history with new date and timestamp)
  changes to "charge" button as follows:
    is only enabled in the following contexts:
        outside of the gift card page
        inside a transaction history if total is less than remaining amount
    inside a transaction history it also functions as a transaction entry append button that uses the current date and timestamp as well as current total
    base functionality remains the same (still clears total but now if in gift card context only after appending a new transaction entry)
  the entire gift card function is completely offline and requires no internet connection whatsoever

[Jun 28 2026]
check modifiers, many have been changed without my consent
gift card function:
  make starting amount and remaining amount text larger
  test to at least 100 entries to ensure function stability
  add export functionality that works with g-drive to upload txt file of current gift card entries
    this needs confirmation from user
  add optional note text field for all transactions
  look into import function
similar note field for custom button/misc items
  if in gift card, custom note becomes part of that transaction
  else, custom note goes in place of "Misc" in items list

[Jul 06 2026]
add 10% add button
change section to disc/tax
make it so custom items become their own transaction